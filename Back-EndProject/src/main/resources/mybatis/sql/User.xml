<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mvc.repository.UserRepository">
    <select id="findByUserId" resultType="org.example.mvc.domain.dto.User.UserInfoDTO">
        SELECT id,userId,name,password,rootPath,totalSize
        FROM user
        WHERE userId = #{userId}
    </select>

    <select id="findByUserName" resultType="org.example.mvc.domain.dto.User.UserInfoDTO">
        SELECT id,userId,name,password,rootPath,totalSize
        FROM user
        WHERE name = #{userName}
    </select>
    
    <insert id="register" parameterType="org.example.mvc.domain.entity.User" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO user(userId,password,name,phoneNumber,email,totalSize,rootPath)
        Values(
        #{userId},
        #{password},
        #{name},
        #{phoneNumber},
        #{email},
        #{totalSize},
        #{rootPath})
    </insert>

    <update id="changePassword" parameterType="map">
        UPDATE user
            SET password = #{nextPassword}
        WHERE userId = #{userId}
    </update>
</mapper>
